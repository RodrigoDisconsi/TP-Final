import { DistinctPacientPipe } from './distinct-pacient.pipe';

describe('DistinctPacientPipe', () => {
  it('create an instance', () => {
    const pipe = new DistinctPacientPipe();
    expect(pipe).toBeTruthy();
  });
});
