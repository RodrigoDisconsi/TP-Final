import { TurnoStatusPipe } from './turno-status.pipe';

describe('TurnoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
