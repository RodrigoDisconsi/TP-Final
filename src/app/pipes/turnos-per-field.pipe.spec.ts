import { TurnosPerFieldPipe } from './turnos-per-field.pipe';

describe('TurnosPerFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosPerFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
